#include <stdio.h>

int main()
{
    char s[34];
    printf("enter your name:");
    gets(s); // function which can be used to receive a multi word string.Eg:- Aakash Gawde
    puts(s);
    // printf("your name is %s", s);
    return 0;
}