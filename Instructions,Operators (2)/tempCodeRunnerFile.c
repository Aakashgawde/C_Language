float qbee, antink, si, prin, anoy, roi;
    qbee = antink + 23.123 / 4.5 * 0.3442;
    si = prin * anoy * roi / 100.0;
    printf("the value of qbee is %f\n", qbee);
    printf("the value of si is %f\n", si);